<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:android="http://schemas.android.com/apk/res/android">

    <item
        android:id="@+id/menu_add_bookmark"
        android:icon="@drawable/add_bookmark_btn"
        android:title="@string/add_bookmark"
        app:showAsAction="ifRoom" />
    <item
        android:id="@+id/menu_clear_all"
        android:title="@string/clear_all"
        app:showAsAction="never" />
    <item
        android:id="@+id/menu_about"
        android:title="@string/about" />
    <item
        android:id="@+id/menu_enable_splash"
        android:checkable="true"
        android:checked="true"
        android:title="@string/enable_splash"
        app:showAsAction="never" />
</menu>